# One-To-One-UniDirectional-Mapping
Spring Boot REST API showcasing One-to-One unidirectional mapping using JPA, Hibernate, and MySQL. Implements layered architecture with CRUD operations, entity relationship management using @OneToOne and @JoinColumn, and demonstrates clean code structure, data persistence, and RESTful service design.
